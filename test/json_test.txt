
# done? - %int%string -> int or string!
#cf) %int%multiple or %int%multiple_4


# remove - just_one, one_more, order_off? or %multiple_on ?
# %int + number -> %int %int %int ...
# array -> %order_on?
# object -> %order_off?
# optional! <> required..
# num of valid var or val??
# check - exist check array?

#%order_on # default, <> use order_off

# object + order_off, array + order_on. ?

$test2 = {
	
}

$Event = {
	id = __init__
	
	# here, change!
	$load_json2 = { { "C:\Users\vztpv\Desktop\Clau\JsonLint\JsonLint\json_test2.txt" } 
	{ test2 }  }
	
	$print = { data = { "init end.." } }
	$print = { enter = { \n } }
	
	$return = { test2 } # returns data folder?
}

$Event = {
	id = int_string
	
	$parameter = { name is_usertype real_dir } # real_dir - ToDo! 
	
	$print = { { $parameter.is_usertype } }
	$print = { { \n } }
	$print = { { $parameter.name } }
	$print = { { \n } }
	$print = { { $parameter.real_dir } }
	$print = { { \n } }
	
	$if = { 
		cond = {
			TRUE
		}
		then = {
			$return = { TRUE }
		}
	}
	
	$return = { FALSE }
}

{
	#"xxx"%optional = %int
	"yyy" = 3

	"x" = {
		"a"%can_empty_ut%one_more%optional = { %order_on %multiple_on
			%can_empty_ut%event_int_string = { %order_on %multiple_on %int%total_id }
		}  
	}
}

